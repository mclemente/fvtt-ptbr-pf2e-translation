{
  "scripts": {
    "build": "gulp build",
    "build:watch": "gulp watch",
    "clean": "gulp clean",
    "clean:link": "gulp link --clean"
  },
  "devDependencies": {
    "fs-extra": "^11.1.1",
    "gulp": "^4.0.2"
  }
}
